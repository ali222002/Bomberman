<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use this build script if -->
<!-- the Compile on Save feature is turned off for the project. -->
<!-- You can turn off the Compile on Save (or Deploy on Save) setting -->
<!-- in the project's Project Properties dialog box.-->
<project name="Lightning" default="default" basedir=".">
    <description>Builds, tests, and runs the project Lightning.</description>
    <import file="nbproject/build-impl.xml"/>
    <path id="classpath">
        <fileset dir="lib" includes="*.jar"/>
    </path>

    <!-- Define the compile target -->
    <target name="compile" description="Compile the source code">
        <mkdir dir="build"/>
        <javac srcdir="src" destdir="build" classpathref="classpath"/>
    </target>

    <!-- Define the jar target -->
    <target name="jar" depends="compile" description="Package the compiled code into a JAR">
        <mkdir dir="dist"/>
        <jar destfile="dist/project.jar" basedir="build">
            <manifest>
                <attribute name="Main-Class" value="com.yourcompany.MainClass"/>
            </manifest>
        </jar>
    </target>

    <!-- Define the test target -->
    <target name="test" depends="compile" description="Run tests">
        <mkdir dir="test-results"/>
        <junit printsummary="yes" haltonfailure="yes">
            <classpath>
                <path refid="classpath"/>
                <pathelement path="build"/>
            </classpath>
            <formatter type="brief" usefile="false"/>
            <batchtest fork="yes" todir="test-results">
                <fileset dir="test" includes="**/*Test*.java"/>
            </batchtest>
        </junit>
    </target>

    <!-- Other existing or additional custom targets can go here -->

</project>
